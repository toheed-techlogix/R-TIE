-- =====================================================================
-- Task: Currency_conversion_of_underlying_acct_attribute_from_natural_CCY_to_reporting_CCY
-- Schema: OFSERM
-- Wrapped: 2026-04-23
-- Source: OFSAA execution log / metadata extraction
-- =====================================================================
CREATE OR REPLACE FUNCTION OFSERM.Currency_conversion_of_underlying_acct_attribute_from_natural_CCY_to_reporting_CCY(
    P_V_BATCH_ID         VARCHAR2,
    P_V_MIS_DATE         VARCHAR2,
    P_V_RUN_ID           VARCHAR2,
    P_V_PROCESS_ID       VARCHAR2,
    P_V_RUN_EXECUTION_ID VARCHAR2,
    P_N_RUN_SKEY         VARCHAR2,
    P_V_TASK_ID          VARCHAR2
) RETURN VARCHAR2 AUTHID CURRENT_USER AS

    ld_mis_date   DATE          := TO_DATE(P_V_MIS_DATE, 'YYYYMMDD');
    ln_mis_date   NUMBER        := TO_NUMBER(P_V_MIS_DATE);
    ln_run_skey   NUMBER(5)     := TO_NUMBER(SUBSTR(P_N_RUN_SKEY, 8, LENGTH(P_N_RUN_SKEY)));
    lv_run_id     VARCHAR2(64)  := SUBSTR(P_V_RUN_ID, 8, LENGTH(P_V_RUN_ID));

BEGIN

    MERGE  INTO FCT_UNDERLYING_DTL TT USING (   SELECT * FROM (  SELECT  /*+ PARALLEL(4) */ FCT_UNDERLYING_DTL.N_CONTRACT_ACCT_SKEY,FCT_UNDERLYING_DTL.N_RUN_SKEY,FCT_UNDERLYING_DTL.N_INSTRUMENT_SKEY,FCT_UNDERLYING_DTL.N_MIS_DATE_SKEY,(FCT_UNDERLYING_DTL.N_PAR_VALUE) AS  T_1346788659324_0,   (CASE  WHEN  ( ((1=1)) ) THEN 10 ELSE 11 END)  AS COND_1346788659324_10 ,  (CASE WHEN 1 = 1 THEN FCT_UNDERLYING_DTL.n_par_value_ncy ELSE NULL END  * CASE WHEN 1 = 1 THEN FCT_UNDERLYING_DTL.n_exchange_rate ELSE NULL END ) AS EXP_1346788659324_10,(FCT_UNDERLYING_DTL.N_PAR_VALUE) AS EXP_1346788659324_11 ,(FCT_UNDERLYING_DTL.N_CURR_MKT_VALUE) AS  T_1346788659324_1,   (CASE  WHEN  ( ((1=1)) ) THEN 20 ELSE 21 END)  AS COND_1346788659324_20 ,  (CASE WHEN 1 = 1 THEN FCT_UNDERLYING_DTL.n_curr_mkt_value_ncy ELSE NULL END  * CASE WHEN 1 = 1 THEN FCT_UNDERLYING_DTL.n_exchange_rate ELSE NULL END ) AS EXP_1346788659324_20,(FCT_UNDERLYING_DTL.N_CURR_MKT_VALUE) AS EXP_1346788659324_21  FROM  FCT_UNDERLYING_DTL LEFT OUTER JOIN DIM_DATES ON FCT_UNDERLYING_DTL.n_mis_date_skey = DIM_DATES.n_date_skey LEFT OUTER JOIN DIM_RUN ON FCT_UNDERLYING_DTL.n_run_skey = DIM_RUN.n_run_skey  WHERE (1=1)  AND (FCT_UNDERLYING_DTL.n_run_skey = '870' AND DIM_DATES.D_CALENDAR_DATE=TO_DATE('20260331','yyyymmdd'))  ) WHERE (   (COND_1346788659324_10 <> 11)   OR  (COND_1346788659324_20 <> 21) ) ) SS ON ( TT.N_CONTRACT_ACCT_SKEY= SS.N_CONTRACT_ACCT_SKEY AND TT.N_RUN_SKEY= SS.N_RUN_SKEY AND TT.N_INSTRUMENT_SKEY= SS.N_INSTRUMENT_SKEY AND TT.N_MIS_DATE_SKEY= SS.N_MIS_DATE_SKEY) WHEN MATCHED THEN UPDATE SET  TT.N_PAR_VALUE=  CASE  WHEN  COND_1346788659324_10=10 THEN EXP_1346788659324_10 ELSE EXP_1346788659324_11 END  , TT.N_CURR_MKT_VALUE=  CASE  WHEN  COND_1346788659324_20=20 THEN EXP_1346788659324_20 ELSE EXP_1346788659324_21 END;

    COMMIT;
    RETURN 'OK';

EXCEPTION
    WHEN OTHERS THEN
        ROLLBACK;
        RETURN 'FAIL: ' || SQLERRM;
END;
/
